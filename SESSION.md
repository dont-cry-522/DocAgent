# 当前状态 — 2026-09-13

## 今日已完成

- 页面恢复黑白灰；简化欢迎区、文字快捷入口和文档页。
- 保留移动端导航、引用折叠、拖拽宽度；增加真实连接与加载错误提示。
- 修正基础 CSS 与 Tailwind 间距冲突，以及输入与错误状态交互。
- 模型名、chat 请求路径、API 模式可配置；增加方舟 Responses 普通/流式适配。
- 方舟 `doubao-seed-evolving` 真实普通和流式请求成功。
- BGE 完整模型文件下载至本地；Embedding Provider 修正为读取配置路径。
- 本地后端启动成功，前端代理及后端健康、文档列表接口返回 200。

模型文件、密钥、运行数据库和日志不随提交发布。其他机器需要自行配置和下载。

## 验证边界

精排未启用；没有完整端到端验收。自动发现 2 项时间格式测试，1 项失败。独立复现确认：历史检索导致新问题不检索、重复上传字段异常、删除元数据中的 None 导致 BM25 异常。

## 下一步（尚未实现）

1. 修复上述三项问题及同名更新失败回滚。
2. 保存并恢复每次回答的引用快照，修正耗时指标和错误终态。
3. 补文档生命周期、多轮问答、重启与模型异常回归测试。
4. 建立检索与回答评估集，再比较改写、混合检索和精排效果。
5. 实现 LLM Planner、章节读取/相邻片段工具、补查与停止条件。
6. 增加上下文预算及运行轨迹；整理启动与 Docker 交付。

当前仍为规则驱动的 RAG 应用，不能将以上规划写成已实现的自主 Agent 能力。
